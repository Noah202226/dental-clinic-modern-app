import { Box, Card, Typography } from '@mui/material'

const PatientList = () => {
  return (
    <Box sx={{ background: 'green', padding: 1 }}>
      <Card>
        <Typography variant="h4">Patient List</Typography>
      </Card>
    </Box>
  )
}

export default PatientList
